
/// Model that represents the `NotifyModel`, with all of its properties and methods.
class NotifyModel {
  /// Property that defines the notification body message
  final String message;

  /// Property that defines the notification ID
  final int notifyID;

  NotifyModel({required this.message, required this.notifyID});
}
